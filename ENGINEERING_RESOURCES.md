# 飞特 Wiki 工程资料验收要求

> 本文件供工程师与 Wiki 维护者使用，不参与网站构建。

型号目录结构、清单字段、双语接入和发布命令统一维护在 [WIKI更新维护教程](WIKI更新维护教程.md)；可填写模板见 [产品型号资料包模板](产品型号资料包模板/README.md)。不要再创建旧式 `docs/products/models/<型号>.md` 单文件页面。

## 程序与结构交付要求

| 客户任务 | 应提供的型号级证据 |
| --- | --- |
| 供电与连接 | 完整型号、允许电压范围、峰值需求与条件、连接器朝向和针序、匹配接口 |
| 总线软件接入 | 固件、适用内存表、SDK 应用层、单位、初始状态、只读示例与硬件验证记录 |
| PWM 软件接入 | 电平、周期、脉宽范围、中位/停止值、角度/转速行为及验证记录 |
| 结构设计 | 尺寸图与 STEP、单位、基准、轴齿/舵盘、孔位/螺纹、装配零位及行程 |
| 负载评估 | 测试条件、持续与峰值能力的证据、温升与工作周期、承载方向及限制 |
| 故障复现 | 实物后缀、固件、代码版本、接线、供电、日志、动作前后状态与停止方式 |

系列相似、同为 TTL 或标称电压相同，都不能替代具体型号的依据。现有规格摘要保留原有数值，新补资料必须追溯到该型号的正式来源；没有资料就标明缺项。

## 曲线资料

原始 JSON/CSV、测试条件和方法共同交付。工装 JSON 的型号字段、测点、量程、单位与序列号原样保留；与具体后缀的映射由工程师核对台账。未知的温度、散热、固件、样本数或供电设定写“未记录”。拟合曲线不作为新增实测点，也不能替代产品连续额定或堵转规格。

## 机械资料

优先提供 STEP 和带公差的 2D 安装图。由机械工程师对照实物校验尺寸、坐标、安装孔、螺钉深度、输出轴与舵盘、线缆出口、机械零位及行程。STL 可用于展示或试装，不提供未经确认的制造公差。图纸和模型冲突时先确认版本，不让客户自行猜测。

## 验收结果如何记录

在型号目录 `manifest.json` 中登记每类附件的状态、文件路径、修订和来源。`available` 只表示已收录文件，不能表示所有客户工况都已验证。示例运行记录写入软件文档或示例附件，装配确认记录写入结构文档，原始测试条件写入 `tests/`。资料清单、中英文正文和共享附件同时更新。

```powershell
.venv\Scripts\python.exe scripts\product_package.py refresh --model st-3215-c001
.venv\Scripts\python.exe scripts\product_package.py validate
.venv\Scripts\python.exe -m mkdocs build --strict
```
