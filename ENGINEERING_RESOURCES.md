# 飞特 Wiki 工程资料与型号页模板

> 本文件供飞特工程师维护仓库资料，不参与 Wiki 网站构建。

## 资料补充重点

| 任务 | 员工应补充的资料 |
| --- | --- |
| 完善产品选型 | 经审核的完整销售型号、有效资料版本和可筛选参数 |
| 评估持续负载 | 扭矩速度曲线、效率、温升测试、连续与峰值工作边界 |
| 完成结构设计 | STEP、2D 安装图、轴齿与舵盘尺寸、公差和安装说明 |
| 接入控制器 | 各系列针序图、配套适配器、可运行示例和测试记录 |
| 排查故障 | 按型号与固件版本整理故障现象、日志和处理步骤 |

曲线需要附测试方法、条件、设备、样本数和原始 CSV。图中标明输入电压、环境温度、负载方式、单位和连续运行条件，并区分实测点、拟合线与额定边界。

3D 模型优先使用 STEP，同时提供 2D 安装图。注明单位、坐标方向、机械零位、安装孔、输出轴或舵盘、线缆出口、版本和许可状态，并由机械工程师对照实物校验关键尺寸。

## 型号产品页模板

复制以下结构到 `docs/products/models/<型号小写文件名>.md`，并在 `docs/en/products/models/` 建立同名英文页面。发布前必须替换所有编辑提示，并复核中英文数字、单位和警示。

```markdown
# 完整销售型号

用一到两句话说明控制方式、适用机构、产品优势和关键限制。

## 关键参数

| 项目 | 数值与单位 | 测试或额定条件 | 资料版本 |
| --- | --- | --- | --- |
| 输入电压 | 待审核 | 待审核 | 待审核 |
| 额定和峰值扭矩 | 待审核 | 待审核 | 待审核 |
| 速度 | 待审核 | 待审核 | 待审核 |
| 尺寸与重量 | 待审核 | 待审核 | 待审核 |

## 供电、接口与接线

说明针序、共地、供电容量、保护要求和配套调试板。总线型号需标明物理接口、默认 ID、波特率及适用内存表版本。

## 安装与运动范围

说明孔位、输出轴或轴齿、舵盘、机械零位、行程和干涉空间，并提供校验后的 STEP 与 2D 安装图。

## 特性曲线

提供曲线图、测试条件、样本数、持续与峰值边界、原始 CSV 和方法说明。

## 控制与开发

列出匹配的通信协议、内存表、固件版本、SDK 应用层和经过实物验证的示例。

## 版本记录

记录页面修订、适用型号、固件版本和审核人。

## 常见问题

仅列出适用于本型号和对应固件版本的已验证排障步骤。
```

新增型号页面后，更新对应系列目录，并执行 `mkdocs build --strict` 和 `git diff --check`。
