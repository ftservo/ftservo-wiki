# FEETECH Wiki 更新与产品资料维护教程

本教程面向资料整理工程师和 Wiki 管理员。目标是让客户从一个完整型号出发，找到程序接入、机械设计和可下载资料，明确每份文件的适用范围。工程师不需要 GitHub 账号，可离线填写资料包后交给管理员。

## 全站框架与内容归属

全站按客户任务组织：**开始使用 → 产品与选型 → 程序开发 → 结构与装配 → 协议与参数 → 故障排查 → 资料下载**。首页提供完整型号 / 别名搜索，以及这几条任务路线。中英文导航在 `mkdocs.yml` 同步修改。

| 内容 | 维护位置 | 边界 |
| --- | --- | --- |
| 首页与任务入口 | `docs/index.md` | 帮客户找到下一步，不重复型号参数 |
| 首次接入 | `docs/getting-started/` | `index.md` 分流，`bus.md` 与 `pwm.md` 各自说明接口路线 |
| 型号规格、控制与安装说明 | `docs/products/models/<id>/` | 完整型号是资料归属单位，后缀与版本必须明确 |
| 选型筛选 | `docs/products/index.md` 和 `docs/javascripts/servo-selector-data.js` | 展示有来源的数据；缺失字段不编造 |
| 共享 SDK 操作 | `docs/sdk/` | 按平台进入、按应用层选择示例；通用教程不等于型号验证记录 |
| 结构设计公共流程 | `docs/mechanical/index.md` | 解释图纸、CAD、装配与软件交接；具体尺寸放型号目录 |
| 协议与内存表 | `docs/reference/` | 区分应用层，型号资料优先 |
| 故障与技术支持 | `docs/troubleshooting.md` | 现象入口、确认结果、支持记录；维护稳定锚点 |

以上每个中文页面在 `docs/en/` 有相同相对路径的英文版本。型号子页通过型号首页和任务入口访问，避免把数百个型号塞入全站侧栏。

### 型号搜索与浏览上下文

- 首页表单将 `model_query` 传给选型页。搜索忽略大小写、空格和连接符，例如 `ST3215` 可匹配 `ST-3215-C001` 和 `ST-3215-C018`，但不会合并这两个型号。
- `q` 由主题全站搜索保留使用，型号搜索使用 `model_query`，避免打开全站搜索弹层。
- 选型筛选保存在 URL：`model_query`、`interface`、`family`、`voltage`、`torque`、`sort`。刷新或复制链接可恢复筛选；重置按钮清除筛选条件。
- 打开型号页面后，`docs/javascripts/developer-context.js` 在同一浏览器标签页记住参考型号。进入 SDK、接线、结构、协议、排障、下载等共享页时显示参考型号，并提供回到型号资料、控制及安装说明的链接。
- 客户可以清除参考型号；共享页也支持 `?model=st-3215-c001` 直接指定现有型号。只接受选型数据里存在的型号。参考型号提示不代表该共享教程已经在此型号上验证。
- PWM 参考型号会提示使用 PWM 路线，避免把总线 ID 和寄存器示例套用到 PWM 接口。
- 语言菜单切换到相同路径的翻译页面，并保留筛选参数；中文和英文标题锚点可能不同，因此切换后从页首阅读。手动语言选择在当前标签页记忆，优先于浏览器语言；本地预览与正式子路径部署使用同一套链接规则。
- 修改共享 CSS、选型数据或交互脚本后，同步更新 `mkdocs.yml` 对应 URL 的 `?v=` 版本标识，再构建页面。当前标识使用文件 SHA-256 前 10 位；可用 PowerShell `Get-FileHash docs/javascripts/servo-selector.js -Algorithm SHA256` 查看。文件内容变化后需更换标识，避免客户继续读到浏览器缓存的旧资源。

### 全站体验验收

1. 从首页搜索 `ST3215`，应看到两个独立后缀；再搜索完整后缀，确认准确结果。
2. 调整筛选，刷新页面或复制链接到新标签页，确认结果保持；重置后恢复全部型号。
3. 从型号页进入共享 Python 或结构教程，检查参考型号与返回链接；清除后刷新，提示不应重新出现。
4. 分别沿总线和 PWM 路线检查入门、首次验证与排障链接；不要给 PWM 路线放串行总线示例。
5. 在英文及手机宽度下重复检查首页搜索、任务导航、参考型号和资料链接。
6. 运行清单验证、工具测试和严格构建。GitHub Pages 工作流在构建前执行资料包验证与工具测试；本地验证不能替代具体型号的硬件联调。

```powershell
.venv\Scripts\python.exe scripts\product_package.py validate
.venv\Scripts\python.exe -m unittest discover -s tests -v
.venv\Scripts\python.exe -m mkdocs build --strict
```

## 1. 型号资料包的新结构

复制根目录的 [产品型号资料包模板](产品型号资料包模板/README.md)，将文件夹改成小写型号 ID，例如 `st-3215-c001`。后缀不能省略，同系列不同后缀不能共用未核实的参数或模型。

```text
<型号-id>/
├─ main.md                 # 客户入口：规格、任务导航、资料下载、支持信息
├─ software.md             # 接口、开发路线、只读联调、验证记录
├─ mechanical.md           # 2D/3D 资料、安装接口、零位、干涉与装配交接
├─ manifest.json           # 文件身份、状态、来源与版本
├─ datasheets/             # 可选：正式规格书
├─ electrical/             # 可选：针序、接线、内存表和固件说明
├─ images/                 # 可选：产品图、接口朝向和装配图
├─ drawings/               # 可选：尺寸图、PDF/DXF/DWG
├─ models/                 # 可选：STEP/IGES/STL
├─ examples/               # 可选：经本型号验证的示例包及说明
├─ tests/                  # 可选：测试条件、方法、原始结果
└─ test-result.json        # 兼容现有曲线文件位置；新增测试也可放 tests/
```

当前 142 个型号的中文页面位于 `docs/products/models/<型号-id>/`，英文位于 `docs/en/products/models/<型号-id>/`，每个目录都有三个 Markdown 页面和一份清单。已有主页面 URL `<型号-id>/main/` 保持不变，型号子页不逐条加入全站侧边栏。

**真实资料现状：**现有目录主要提供规格摘要和集成检查步骤，尚未收录型号专用 STEP、2D 安装图、针序图及验证示例。[HLS3950 案例](docs/products/models/hl-3950-c001/main.md)另有原始测试 JSON 和测试记录；它是曲线接入案例，不是完整工程交付样板。缺项在页面中如实显示“待补充”，不放失效下载按钮或破图。

可选目录有真实文件后再加入 Wiki，无需为每个型号创建一套空目录。资料包模板的 `en/` 是翻译交付区，**不是**最终网站目录；接入时按第 5 节拆分。

## 2. 三个页面分别写什么

### main.md：客户的型号入口

- 完整标签型号、目录型号及资料版本；必要时说明两者命名映射。
- 保留经确认的规格数值、单位及条件；电压单值不自动推导范围，堵转值不写成连续额定值。
- “程序开发”“结构设计”“资料下载”入口。
- 真实附件清单；曲线仅在有原始数据时启用。
- 申请缺失资料与反馈问题时需要提交的信息。

资源表位于 `<!-- product-resources:start -->` 和 `<!-- product-resources:end -->` 之间，由清单刷新生成。工程师只修改清单，其他正文可正常编辑，不要删除标记。规格正文不会被刷新命令覆盖。

### software.md：让软件开发者知道下一步

- 写明物理接口、针序图方向、供电、共地和对应适配器。
- **PWM**：确认信号电平、周期、脉宽范围、中位/停止值和角度/转速模式，不填串行总线 ID、波特率或 SDK 寄存器。
- **总线**：确认实际 ID、波特率、固件版本、SDK 应用层、寄存器单位与模式，先 Ping 和只读查询，再验证小动作。
- SCS、STS/SMS、HLS 的系列入口分别对应 SCSCL、SMS_STS、HLSCL；必须进一步核对具体型号内存表。HD 等尚未确认应用层的型号不能仅因同为 TTL 就套用其他类。
- 示例要有平台、依赖安装、执行方法、SDK 提交号、实物完整型号、固件、预期结果、停止方式与硬件验证记录。
- 未经该型号实物测试的共享教程不能标注成“本型号已验证示例”。多文件示例可作为一个 ZIP 登记，必须包含依赖和运行说明。

### mechanical.md：让结构设计者知道能否出图

- 提供型号及版本匹配的尺寸图和 STEP，明确单位、坐标原点、安装基准及机械零位。
- 核实孔距、孔径、螺纹、有效旋入深度、轴齿/舵盘、从动轴、连接器和出线空间。
- 写明可用行程、方向、限位、紧固要求和负载条件；未知的径向/轴向承载及连续能力保持缺项。
- STEP 用于装配检查，公差以批准的尺寸图为准；STL 网格不能当作制造公差依据。
- 给软件人员留下安装零位、允许行程、方向、装配版本和干涉截图。

资料到齐后，同时更新正文中的“尚未收录”说明，避免清单显示可下载而正文仍然说缺失。不要根据相似型号补写尺寸、电流、分辨率或寄存器。

## 3. manifest.json 怎么填

两种语言使用相同清单，附件也保存相同字节，避免中文升级而英文仍在下载旧版本。需要多语言工程文件时，可提供一个含两种语言文件的 ZIP，并共同指向该文件。

| 字段 | 要求 |
| --- | --- |
| `schema_version` | 当前为 `1` |
| `model_id` | 与文件夹名一致的小写 ID |
| `model_name` | 实物/文档完整名称，不自行补后缀映射 |
| `family` / `interface` | 经确认的系列及物理接口 |
| `document_revision` | 型号资料版本；未知用 `null` |
| `sources` | 来源记录数组：名称、版本、适用型号、页码或章节等 |
| `assets` | 保留八类资源条目，按实际交付状态填写 |

八类资源：`datasheet`、`pinout`、`memory_table`、`drawing`、`cad`、`example`、`test_result`、`test_conditions`。状态含义：

- `missing`：未提供或待确认，`path` 必须为 `null`。
- `available`：已收录，`path` 指向本型号目录中的真实非空文件，`source` 必须说明来源。
- `not_applicable`：明确不适用，如 PWM 的总线内存表；不等于“还没有收集”。

附件更新示例（只有拿到真实文件才照此修改）：

```json
{
  "kind": "cad",
  "status": "available",
  "path": "models/housing-r1.step",
  "revision": "R1",
  "source": "填写正式图号、适用完整型号、批准版本与来源记录"
}
```

禁止跨型号引用 `../其他型号/`，禁止本机绝对路径。使用小写英文文件名和连字符，尽量保留修订号，避免同名覆盖。一个资源类有多个文件时，登记一个包含清单与版本说明的 ZIP，或主文档；同型号附件的补充链接可写在正文中。

目前清单中的 `existing_catalog` 只追溯既有目录数据，**不是**本次审核或正式工程资料的证明。补充批准文件时，应增加真正的来源记录。

## 4. 实测曲线和测试记录

优先保存工装原始 JSON，不手改测点、单位、序列号或时间。组件需要 `measurements` 以及 `range_profile.axes`，包含扭矩、转速、电流、效率和功率的 `min`、`max`、`step` 和小数位。

```html
<div class="servo-characteristic-chart" data-source="../test-result.json" data-title="实际型号 T-N 实测特性曲线"></div>
```

如果数据位于 `tests/run-001.json`，使用 `data-source="../tests/run-001.json"`。这是 **HTML 在构建后的 `main/` 页面上的相对 URL**；Markdown 下载链接则写 `[原始数据](tests/run-001.json)`，不要混淆两种路径。

清单的 `test_result` 条目还需填写 `test_model`，内容与原始 JSON 的 `model` 完全一致。管理员必须核实测试台账与实物后缀的关系；自动一致性检查不能认证样本型号。例如现有 JSON 为 `HLS3950M`，未记录 C001 后缀，测试记录已明确这一限制。

当前显示沿用工装前端：

- 扭矩为横轴，四项指标共用绘图区，分别使用彩色独立纵轴和 JSON 固定量程。
- 按采集顺序截取首次峰值前数据；原始 JSON 保留不变，页面注明被隐藏的点数。
- 转速、电流、功率使用鲁棒 LOWESS；效率使用工装的保形插值；平滑曲线输出 181 点。
- 可切换原始折线/平滑趋势；悬浮、触摸或键盘选点查看五项参数，小屏可横向滚动。
- 平滑值、原始实测值与产品规格分开，不从测试曲线推断连续额定能力。

将测试日期、完整样本身份、固件、电源、环境、安装负载、工装校准、持续时间、结束条件和缺项写入 `tests/` 的测试说明。模板提供空白记录；缺失条件写“未记录”，不得用猜测补齐。

## 5. 工程师交付与管理员接入

1. **工程师整理：**复制模板、改型号、填写三个中文页面、英文对应页和清单；用 Markdown 阅读器检查正文。直接双击 Markdown 或 `file://` 打开页面不能验证交互曲线。
2. **审核附件：**核对实物后缀、文档版本、许可与来源，检查程序示例和 CAD；收到文件后才添加链接。发布目录中不能包含客户私有资料、凭据或未发布草稿。
3. **中文接入：**将三个页面、清单及确认发布的附件放入 `docs/products/models/<型号-id>/`，不复制模板 README、空白记录和编辑草稿。
4. **英文接入：**将模板 `en/` 内的三个页面及清单放入 `docs/en/products/models/<型号-id>/`，将共享附件按相同相对路径复制过去。中英文页面必须同时存在。
5. **目录入口：**更新中英文 `docs/products/datasheets/<系列>.md`；其中 HTML 卡片的主页面链接为 `../../models/<型号-id>/main/`。Markdown 文件之间使用 `.md` 源文件链接。
6. **选型器：**在 `docs/javascripts/servo-selector-data.js` 加入/更新条目，`detail` 为 `models/<型号-id>/main/`；若新增型号，同步页面显示的型号数量。不要把新型号的每个子页都塞进 `mkdocs.yml` 的侧栏。
7. **刷新与检查：**在仓库根目录执行下面命令。新资料只刷新清单区域，不覆盖工程师写的规格和集成说明。

```powershell
# 只刷新一个型号；省略 --model 会刷新全部
.venv\Scripts\python.exe scripts\product_package.py refresh --model st-3215-c001

# 检查全部型号的目录、双语清单、真实附件与资源表
.venv\Scripts\python.exe scripts\product_package.py validate

# 严格构建中英文站点，检查链接
.venv\Scripts\python.exe -m mkdocs build --strict

# 浏览器预览，包括图片、下载和曲线
.venv\Scripts\python.exe -m mkdocs serve -a 127.0.0.1:8000
```

打开 `http://127.0.0.1:8000/products/models/<型号-id>/main/`，检查两个任务入口、资源链接、英语页面及小屏表现。曲线还要检查模式切换与读数。停止服务按 `Ctrl+C`。

## 6. 交付离线资料包

工具只导出这三个页面、清单、登记为可用的附件和正文中引用的本地图片，不会把整个工作目录及草稿扫入 ZIP。多文件代码或工程项目需要先打成明确的附件 ZIP，再登记到清单。

```powershell
.venv\Scripts\python.exe scripts\product_package.py package --model hl-3950-c001 --output exports\hl-3950-c001.zip
```

导出包包含 `zh/`、`en/`、双语阅读说明及 `SHA256SUMS.txt`。共享 SDK/接线教程和交互曲线通过在线 Wiki 使用，包内不包含 SDK 子模块。需要完整离线 SDK 时，按下载中心使用 `git clone --recurse-submodules`。

目标 ZIP 已存在时工具会拒绝覆盖；请使用新的版本文件名。导出目录 `exports/` 已加入 Git 忽略规则，也不要将 ZIP 放回 `docs/` 发布旧副本。

## 7. 常见维护问题

| 检查结果 | 处理方法 |
| --- | --- |
| `missing main.md / software.md / mechanical.md / manifest.json` | 按模板补齐，两种语言都要有 |
| `resource table out of date` | 修改清单后运行 `refresh` |
| `bilingual manifests differ` | 核对型号、状态、路径、版本、来源，两份清单同步 |
| `bilingual asset bytes differ` | 同时更新两种语言的共享附件，不仅改文件名 |
| `Missing or empty asset` | 放入实际文件，或恢复为 `missing` 和 `path: null` |
| `Test JSON model ... test_model` | 核对原始导出与台账，再填写已核实的型号映射，不修改原始 JSON |
| `mkdocs build --strict` 链接告警 | 区分 Markdown 源文件路径与 HTML 构建后 URL，核对大小写和资源是否存在 |
| CAD 已到但结构页仍说缺失 | 同步更新正文，并补充单位、基准与适用版本 |

## 8. 发布前最后复核

- 型号、电压、控制限制、尺寸与来源可追溯；未经确认的信息明确标为缺项。
- 程序路线区分 PWM 与不同总线应用层；实物测试注明准确型号与条件。
- 结构图纸、模型、舵盘、安装零位与允许行程匹配，缺失资料不伪装成下载项。
- 清单检查、严格构建、页面与附件预览通过，中文和英文同步。
- 审阅 `git diff`，不提交 `site/`、虚拟环境、缓存、导出包、凭据或无关改动。

仓库已经存在的未提交改动应由维护者一起审阅，不要用全目录覆盖或批量还原破坏其他人的工作。
